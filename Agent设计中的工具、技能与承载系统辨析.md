# Agent 设计中的 Tools、Skills 与 Harness 辨析

## 0. 为什么要区分这三者

在设计一个 AI Agent（智能体）时，很容易把"给模型加能力"这件事简单地理解为"多写几个函数让它调用"。但实践下来会发现，很多问题不是靠加函数能解决的——有些问题应该用一段说明文档来解决，有些问题根本不该由模型本身解决，而应该由模型外面那层运行时来解决。

这三个概念对应的正是三个不同的层次：

| 层次 | 一句话定义 | 回答的问题 |
|---|---|---|
| **Tool（工具）** | 模型可以调用的、有固定输入输出结构的函数 | "Agent 能不能做这件事？"（能力问题） |
| **Skill（技能）** | 按需加载进上下文的自然语言知识/流程包 | "Agent 该怎么把这件事做好？"（方法问题） |
| **Harness（承载系统 / 运行时）** | 包裹在模型外面、决定 Agent 如何运转的基础设施 | "Agent 是在什么样的环境里运转的？"（环境问题） |

三者是**分层**关系而非并列关系：Harness 是最外层的容器，它决定了 Tools 如何被注册和调用、Skills 如何被发现和加载；Tools 和 Skills 都运行在 Harness 划定的边界之内。

```
┌─────────────────────────────────────────────┐
│                  Harness                     │
│   （上下文管理 / 权限系统 / 多智能体编排 /      │
│     持久化 / 调度 / 工具路由）                 │
│                                               │
│   ┌──────────────┐      ┌──────────────┐     │
│   │    Tools     │      │    Skills    │     │
│   │ （能力/动作） │      │（知识/流程）  │     │
│   └──────────────┘      └──────────────┘     │
│              ↑ 都被 Model 在循环中调用 ↑        │
└─────────────────────────────────────────────┘
```

---

## 1. Tool：能力的边界

### 1.1 定义

Tool 是模型可以按结构化协议调用的函数：有明确的 JSON Schema 描述输入参数，执行后返回结果供模型继续推理。常见例子：`Read`、`Write`、`Bash`、`Grep`、调用外部 API、查询数据库、发送消息。

### 1.2 特点

- **确定性执行**：Tool 一旦被调用，执行逻辑是代码写死的，不依赖模型的"理解"——模型只负责决定"调不调、传什么参数"，不负责"怎么执行"。
- **结构化契约**：入参、出参都有 Schema 约束，适合需要精确性、幂等性、可审计性的操作。
- **常驻上下文成本**：每个注册的 Tool 的 Schema 通常会占用一部分上下文窗口（不管用不用），所以 Tool 数量不是越多越好。也因此出现了"延迟加载工具"（deferred tools）的模式——先只暴露名字，真正需要时再用类似 `ToolSearch` 的机制拉取完整 Schema。
- **代表"能力的天花板"**：如果一个 Agent 没有"执行代码"这个 Tool，无论提示词写得多好，它都不可能真正跑代码。Tool 决定了 Agent **能不能**做某件事，这是提示词或文档无法弥补的硬约束。

### 1.3 什么时候应该新增一个 Tool

- Agent 需要与外部系统交互（文件系统、数据库、第三方 API、Shell），而这个交互本身需要真实执行、而非模型"脑补"。
- 这个操作需要严格的输入校验、错误处理、幂等性保证——例如金融交易、删除操作、写库操作。
- 这个操作的结果不应该由模型的"文本生成"来伪造，必须有真实的外部世界反馈（例如运行测试、查询实时数据）。

### 1.4 常见误用

- 把"格式化文本""生成一段总结"这种模型本来就擅长的纯语言任务也包装成 Tool——这是多此一举，模型直接输出即可。
- 为了一个很少用的边缘功能常驻注册一个 Tool，拖慢每次请求的上下文加载，正确做法通常是做成延迟加载或者放进 Skill 里按需展开。
- 用一堆细粒度 Tool 去拼装本该是"一段说明"就能解决的流程——这通常是 Skill 该管的事，硬塞成 Tool 会让模型的调用变得机械、脆弱，遇到没预料到的情况就卡住。

---

## 2. Skill：知识与流程的封装

### 2.1 定义

Skill 是一段可以被"按需加载"到上下文里的自然语言知识包（通常配合脚本、模板、参考资料），它不提供模型原本没有的能力，而是**教模型如何更好地使用已有能力**去完成某类任务。典型形式：一个带 YAML frontmatter（name + description）的 `SKILL.md`，运行时根据任务描述判断是否要加载。

### 2.2 特点

- **不是新能力，是新知识**：Skill 里描述的操作，理论上模型不用 Skill 也能勉强做，但效果参差不齐、容易漏步骤、风格不统一。Skill 的价值在于把"最佳实践"沉淀下来，反复复用。
- **按需加载，节省上下文**：只有当任务与 Skill 的 `description` 匹配时才会被拉进上下文，不用时不占用窗口——这是它和 Tool 在"上下文经济学"上的关键差异：Tool 的 Schema 通常是常驻的，Skill 是渐进式披露（progressive disclosure）的。
- **可以组合调用 Tool**：Skill 内部经常会指导模型按顺序调用哪些 Tool、传什么参数、如何处理异常情况——Skill 是"剧本"，Tool 是"演员能做的动作"。
- **面向可复用的方法论**：适合封装团队约定俗成、但不成文的流程，比如"如何做代码评审""如何写数据可视化""如何做深度调研"。

### 2.3 什么时候应该写一个 Skill

- Agent 已经具备完成任务所需的全部 Tool，但反复出现"做得不到位""漏掉某个步骤""风格不统一"的问题——说明缺的是流程指导而非能力。
- 某类任务有清晰、可复用的最佳实践，值得沉淀（例如："写图表前先看配色规范""审查 PR 前先跑一遍 checklist"）。
- 你希望不同场景下按需拉入不同的专业知识，而不是让模型在没有相关任务时也背着这些说明。

### 2.4 常见误用

- 把 Skill 当成 Tool 的替代品，试图用一段说明文字让模型"假装"执行了一个本该真实发生的操作（比如假装发了邮件、假装跑了测试）——凡是需要真实副作用或真实反馈的，必须落到 Tool。
- Skill 写得过于死板、像伪代码一样规定每一步，反而丧失了模型灵活应对边缘情况的优势——好的 Skill 通常介于"完全放任"和"逐字脚本"之间，给出原则、示例、决策依据，而非机械步骤。
- 用 Skill 去尝试修正 Harness 层面的问题（例如上下文管理不当、权限模型有缺陷）——这类问题写多少说明文档都没用，需要在运行时层面解决。

---

## 3. Harness：Agent 运转的基础设施

### 3.1 定义

Harness 是包裹在模型之外、决定 Agent **如何**运转的整套运行时系统：上下文窗口管理与压缩、工具的注册与路由、权限与审批机制、多智能体编排（子智能体、并行 workflow）、会话与记忆持久化、后台任务与调度、Hook 机制等等。Claude Code 本身就是一个 Harness 的具体实现；Claude Agent SDK 则是让你自己搭建 Harness 的框架。

### 3.2 特点

- **决定"游戏规则"，而非"游戏内容"**：Tool 和 Skill 都是在 Harness 划定的边界内运作的——Harness 决定了一次对话能容纳多少上下文、超出后如何压缩、工具调用失败如何重试、用户何时需要被打断确认。
- **模型对 Harness 通常是"无感"的**：模型不需要知道自己的对话历史被自动压缩过，也不需要知道某个 Tool 是"延迟加载"的——这些都是 Harness 在背后做的事。
- **决定了 Agent 的"物种"而非"个体差异"**：同一个模型，装在一个只能单轮问答的 Harness 里和装在一个能跑后台任务、多智能体协作、持久化记忆的 Harness 里，行为形态是完全不同的——这不是靠换提示词能弥补的差距。
- **典型 Harness 能力举例**（对照本环境）：
  - 上下文自动压缩（长对话超限后自动摘要）
  - 权限模式与审批（不同操作需要不同程度的用户确认）
  - 子智能体编排（Agent / Workflow 工具，支持并行、流水线、隔离 worktree）
  - 持久化记忆系统（跨会话保留用户偏好、项目背景）
  - 后台任务与调度（ScheduleWakeup、CronCreate、Monitor）
  - 工具的渐进式披露（deferred tools + ToolSearch）

### 3.3 什么时候问题出在 Harness 层，而不是 Tool 或 Skill 层

- Agent 频繁"忘记"早前的上下文——这可能是压缩策略或记忆机制的问题，不是加个 Tool 或 Skill 能解决的。
- 需要多个 Agent 并行协作、且彼此结果要汇总校验——这是编排能力，需要 Harness 提供的多智能体框架（如 Workflow），而不是靠单个 Agent 里堆砌 Tool。
- 某些操作需要"人工审批后才能继续"——这是权限系统该管的事，而不是靠在 Skill 里写"请先询问用户"（模型可能会忘、也可能被注入攻击绕过，真正的强制力必须来自 Harness 的权限拦截）。
- 需要长时间运行、定时触发、或者在后台等待外部事件——这是调度和任务管理能力，属于 Harness。

### 3.4 常见误用 / 认知误区

- 把 Harness 问题误诊为"模型不够聪明"，试图靠换更强的模型或者写更长的提示词解决——例如上下文丢失、工具调用被截断，这些往往是运行时设计缺陷，模型再强也绕不开。
- 自己从零搭建 Harness 时低估复杂度：权限、压缩、并发、错误恢复都要考虑，而这些工程问题往往比"设计一个好 Tool/Skill"复杂得多。

---

## 4. 设计 Agent 时如何三选一（决策框架）

面对一个新需求，可以按下面的顺序自问：

```
这个需求是关于……

1. "环境/运行时本身该如何工作"吗？
   （上下文怎么管、权限怎么审、多个 Agent 怎么协同、任务怎么调度）
   → 是 → Harness 层面的设计，需要改运行时/框架，不是加 Tool 或写 Skill 能解决的

2. 排除 1 之后：Agent 缺的是"实际执行某个动作的能力"吗？
   （读写文件、调用外部系统、执行代码、产生真实副作用）
   → 是 → 设计一个 Tool

3. 排除 1、2 之后：Agent 已经有能力，但不知道"怎么做才好/才对/才符合规范"吗？
   （流程、风格、检查清单、领域知识、最佳实践）
   → 是 → 写一个 Skill
```

### 4.1 一个简单的判断句式

> "如果没有这个东西，Agent 是**结构上做不到**，还是**做得到但做得差**？"
> - 结构上做不到 → 缺 Tool（或者更底层，缺 Harness 支持）
> - 做得到但做得差/不稳定/没有章法 → 缺 Skill

### 4.2 举例对照

| 需求 | 判断 | 原因 |
|---|---|---|
| "让 Agent 能读写本地文件" | Tool | 需要真实的文件系统访问能力 |
| "让 Agent 写代码评审时按统一维度检查" | Skill | Agent 本来就能读代码、能推理，缺的是流程规范 |
| "让 Agent 在多个文件上并行跑独立子任务" | Harness | 需要并发编排、隔离执行环境的运行时能力 |
| "让 Agent 调用天气 API" | Tool | 需要真实网络请求与外部数据 |
| "让 Agent 画图表时配色专业、风格统一" | Skill | 已有渲染能力，缺的是设计知识 |
| "让 Agent 长对话不丢失早期上下文" | Harness | 需要上下文压缩/记忆机制，不是知识或能力问题 |
| "让某个危险操作必须用户确认后才执行" | Harness（权限系统）+ 提示词提醒 | 强制力必须来自运行时拦截，提示词只能是"建议"，不能兜底 |

### 4.3 组合使用是常态

真实场景往往是三者配合：

- 一个"自动化代码审查" Agent：
  - **Harness** 提供子智能体并行编排（多个维度同时审查）、权限系统（评审意见发布前是否需要确认）；
  - **Skill**（如 `code-review`）规定审查维度、评分标准、输出格式；
  - **Tool**（`Grep`、`Read`、`Bash` 跑测试）提供实际读取代码、执行验证的能力。

三者相互独立又相互依赖：改进 Skill 不需要改 Tool 的 Schema；扩展 Tool 不需要重写 Harness；但 Harness 的边界（比如上下文预算、并发上限）会限制 Tool 和 Skill 能被怎样使用。

---

## 5. 与之相关的几个延伸话题

### 5.1 Subagent（子智能体）与 Skill 的区别

两者都是"复用能力"的机制，但运作方式不同：

- **Skill** 在**当前对话的同一个上下文**里加载，共享同一份历史，模型直接看到 Skill 内容后继续推理。
- **Subagent** 在**独立/隔离的上下文**里运行，只接收你显式传给它的 prompt，跑完之后把结果（通常是一段总结）返回给主对话——主智能体看不到子智能体执行过程中的细节。

选择依据：如果任务需要保留在主上下文里持续追问、迭代，用 Skill；如果任务体量大、容易"污染"主上下文（比如翻遍几十个文件做调研），适合丢给 Subagent 做，只把结论带回来。

### 5.2 MCP（Model Context Protocol）与 Tool 的关系

MCP 是一种标准协议，用来把外部系统（Notion、Linear、数据库等）的能力暴露成一组 Tool，接入到 Harness 里。可以理解为：MCP 是"如何把第三方能力包装成 Tool 并接入 Harness"的标准化方案，而不是与 Tool 平行的第四个概念——它是 Tool 的一种来源和封装形式。

### 5.3 上下文经济学：为什么 Tool 和 Skill 的"加载时机"不同

- Tool 的 Schema 传统上是**请求发起前就确定并常驻**的（模型需要提前知道有哪些工具可用、参数长什么样），所以数量一多就侵占上下文。渐进式披露（先给名字，按需展开 Schema）是缓解这个问题的工程手段。
- Skill 天生就是**渐进式披露**的设计：先只加载一行 `description` 用于匹配，命中后才展开全文——这也是为什么 Skill 更适合承载"体量较大、但只有小部分场景用得上"的知识。

设计新能力时，这个"何时加载进上下文"的成本，是选 Tool 还是 Skill 的一个隐藏但重要的考量维度。

### 5.4 权限与安全边界应该设在 Harness，而不是寄望于提示词

一个容易踩的坑：试图靠在 Skill 或系统提示词里写"执行危险操作前请务必询问用户"来做安全兜底。这类约束只是"建议"，模型可能因为提示注入、上下文丢失、或者单纯推理失误而忽略。真正需要保证"这类操作一定会被拦下来给人确认"时，必须由 Harness 的权限系统在代码层面强制执行（审批弹窗、白名单/黑名单、只读沙箱等），不能只靠自然语言约束。

### 5.5 从 Claude Code 自身看这套分层（案例参考）

以你现在正在使用的环境为例：

- **Tool**：`Read`、`Write`、`Edit`、`Bash`、`Grep`、`Glob` 等——纯粹的执行能力。
- **Skill**：`code-review`、`dataviz`、`deep-research` 等——教模型如何把已有能力用得更专业。
- **Harness**：上下文自动压缩机制、权限确认弹窗、`Agent`/`Workflow` 的子智能体编排、`ScheduleWakeup`/`CronCreate` 的后台调度、跨会话的记忆系统——这些共同构成了"Claude Code 是什么样的 Agent 环境"。

这三层各自独立演进：新增一个 Tool 不影响 Skill 的组织方式；写一个新 Skill 不需要触碰权限系统；而 Harness 的每一次升级（比如更聪明的压缩策略、更大的并发上限）会同时提升所有 Tool 和 Skill 的实际表现上限。

---

## 6. 小结

- **Tool 回答"能不能"**：没有它，事情在结构上做不到。
- **Skill 回答"好不好"**：有能力但缺章法、缺经验时补上。
- **Harness 回答"在什么环境里跑"**：决定上下文、权限、编排、调度这些游戏规则，是前两者赖以运作的地基。

设计 Agent 时最常见的误区，是把本该属于 Harness 的问题（安全边界、上下文管理、并发编排）硬塞进提示词或 Skill 里去"祈祷"模型遵守；其次常见的误区，是把本该属于 Skill 的方法论问题，误以为要靠堆砌更多 Tool 来解决。分清楚问题该落在哪一层，往往比"再优化一下提示词"更能从根本上解决问题。
